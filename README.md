# Duo

React + TypeScript + Vite. Phone-first, black background, white text.

## Development

```
npm install
npm run dev
```

## Build

```
npm run build
```

Builds to `docs/`, published via GitHub Pages from `main` → `/docs`.
